/target
**/*.rs.bk
Cargo.lock.bak

# Uncommitted local cross-repository development override (see docs/development.md).
# The path-override file MUST NOT be committed; released builds resolve the
# shared runtime from a tagged version, never a sibling relative path.
.cargo/config.toml
